<div id="<%= dom_id post %>">
  <p>
    <%= link_to display_name(post.user), users_profile_path(post.user) %>
  </p>

  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>
</div>
